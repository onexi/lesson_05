# Generates a stream of animal names, one per line, on standard output.
# The sample stream contains five occurrences of cat and three of dog.
# Provides the input for the filtering stage of the pipeline.
