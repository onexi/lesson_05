# Coordinates three agents running in parallel.
# Creates an output directory and captures each agent's output in a separate file.
# Reports elapsed time and whether each agent is still running.
# Waits for all agents to finish before combining their outputs into results.txt,
# in agent order.
