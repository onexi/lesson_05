# Acts as the reader in a tiny newsroom built with a named pipe.
# Creates a named pipe called newsroom and waits for incoming headlines.
# Continuously reads and displays headlines with a newspaper symbol.
# Removes the named pipe when the editor exits.
