# Reads a startup idea from standard input and uses Codex to turn it into
# an investor pitch consisting of five slides.
# Each slide has a strong title and no more than three short bullets.
# Writes only the five slides as text to standard output.
